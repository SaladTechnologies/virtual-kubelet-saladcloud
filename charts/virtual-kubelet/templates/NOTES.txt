The SaladCloud Virtual Kubelet provider is getting deployed on your cluster.

To verify that the Virtual Kubelet has started, run:

  kubectl --namespace={{ .Release.Namespace }} describe deployment "{{ .Chart.Name }}"
